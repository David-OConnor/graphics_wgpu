[package]
name = "graphics"
version = "0.1.0"
authors = ["David O'Connor <david.alan.oconnor@gmail.com>"]
edition = "2021"

[dependencies]
chrono = "^0.4.19"

# 3D rendering, using Vulkan
#ash = "^0.36.0+1.3.206"

wgpu = "^0.13.1"
#wgpu = { git = "https://github.com/gfx-rs/wgpu" }  # Use GH version so examples work.

cgmath = "0.18.0" # todo temp to TS issues

# That's a lot of deps for WGPU examples. Try to trim it down?

winit = "^0.26.1"
bytemuck = {version = "^1.11.0", features = ["derive"]}
ddsfile = "^0.5.0"
pollster = "^0.2.5"  # Todo: Remove if able.
obj = "^0.10.2"
env_logger = "^0.9.0"
log = "^0.4.14"
image = "^0.24.1"
anyhow = "1.0.56" # seriously - get rid of this.